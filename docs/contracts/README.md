# 跨服务契约

本目录存放跨服务、可版本化的 API、事件和数据契约。

技术选型完成后，在此提交 OpenAPI、AsyncAPI 或 schema 文件；接口兼容性、版本策略和破坏性变更须记录 ADR。

## 契约索引

- [0001：service-api User/Auth OpenAPI](0001-service-api-user-auth.openapi.yaml) — Implemented

同步服务内部 API 将在后续独立编号方案中设计与实现。
