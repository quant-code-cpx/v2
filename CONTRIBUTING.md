# 参与开发

## 开始前

1. 阅读根目录及目标目录中的 `AGENTS.md`。
2. 确认改动只覆盖一个清晰目标。
3. 架构或跨服务变更先创建 ADR。
4. 从 `.env.example` 创建本地 `.env`，不要提交真实密钥。

## 开发要求

- 保持服务边界清晰，避免跨目录共享内部实现。
- 新功能配套测试，缺陷修复配套回归测试。
- 新增环境变量、接口或运行方式时同步更新文档。
- 提交前运行受影响服务定义的格式化、静态检查和测试。

## 提交与 PR

- 建议使用 Conventional Commits。
- 一个提交只解决一个问题。
- PR 应说明背景、变更、验证结果、风险和回滚方式。
- 不把格式化全仓库、依赖升级和功能变更混在同一个 PR。

技术栈确定后，应在各服务 README 中补充具体开发命令。

